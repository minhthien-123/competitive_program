#include <bits/stdc++.h>

int main(){
    std::ios_base::sync_with_stdio(0);
    std::cin.tie(0); std::cout.tie(0);
    std::cin >> n;
    for (int i = 1, x; i <= n; i++){
        std::cin >> x;
        if (a [i] % 2 == 0) a[i] = 1;
        else
    }

}
