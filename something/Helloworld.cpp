#include <bits/stdc++.h>

int n;

int main(){
    std::ios_base::sync_with_stdio(0);
    std::cin.tie(0); std::cout.tie(0);

    std::freopen("abc.inp", "r", stdin);
    std::freopen("abc.out", "w", stdout);

    std::cin >> n;
    std::cout << n;
}
